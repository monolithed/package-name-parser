enum ErrorMessage {
    EMPTY = 'Name cannot be an empty value',
    BLACKLIST = 'Name cannot contain blacklisted names',
    BUILTINS = 'Name cannot contain builtin names',
}

export {ErrorMessage};
